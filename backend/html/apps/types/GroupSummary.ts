export default interface GroupSummaryType {
    comments_count: number,
    comments_per_post: number,
    likes_per_post: number,
    posts_count: number,
    reposts_per_post: number,
    views_per_post: number,
}